@model PruebaUmbracoComposition.Web.ViewModel.GetHeroDesdeHomeViewModel

<div class="jumbotron jumbotron-fluid home-hero" style="background-image:url('@Model.HeroImageUrlDesdeHome')">
    <div class="inner">
        <div class="container">
            <h1 class="display-4">@Model.HeroTitleDesdeHome</h1>
            <p class="lead">@Model.HeroSubtitleDesdeHome</p>
        </div>
    </div>
</div>
