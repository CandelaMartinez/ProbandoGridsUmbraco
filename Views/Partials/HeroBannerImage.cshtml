@model PruebaUmbracoComposition.Web.ViewModel.HeroBannerViewModel


<div class="jumbotron jumbotron-fluid home-hero" style="background-image:url('@Model.HeroImageUrl')">
    <div class="inner">
        <div class="container">
            <h1 class="display-4">@Model.HeroTitle</h1>
            <p class="lead">@Model.HeroSubtitle</p>
        </div>
    </div>
</div>

